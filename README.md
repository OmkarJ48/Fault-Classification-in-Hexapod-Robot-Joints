# Fault-Classification-in-Hexapod-Robot-Joints
ML pipeline for multiclass fault detection in hexapod robot joints — K-Means/DBSCAN clustering, SVM/Random Forest/KNN/neural network classification, SMOTE for class imbalance, evaluated with cross-validation, F1 and ROC AUC. MSc coursework, MATLAB.
